# Lab 56 Exercise 6: Pod with dnsPolicy ClusterFirst (baseline)
apiVersion: v1
kind: Pod
metadata:
  name: dns-clusterfirst
  namespace: lab56-dns
spec:
  dnsPolicy: ClusterFirst
  containers:
    - name: app
      image: busybox:1.36
      command: ["sleep", "3600"]
