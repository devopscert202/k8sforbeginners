# Lab 56 Exercise 6: Pod with dnsPolicy Default (node resolver)
apiVersion: v1
kind: Pod
metadata:
  name: dns-default-node
  namespace: lab56-dns
spec:
  dnsPolicy: Default
  containers:
    - name: app
      image: busybox:1.36
      command: ["sleep", "3600"]
